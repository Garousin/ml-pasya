# Architecture Diagrams

## System Overview - Scalable ML API

```
┌─────────────────────────────────────────────────────────────────┐
│                    CLIENT APPLICATIONS                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ Laravel  │  │ Mobile   │  │   Web    │  │  Admin   │       │
│  │   App    │  │   App    │  │   App    │  │  Panel   │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
└───────┼─────────────┼─────────────┼─────────────┼──────────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                      │ HTTP/JSON
        ┌─────────────▼──────────────────────────────────────┐
        │          LOAD BALANCER (Optional)                  │
        │         Nginx / HAProxy / AWS ALB                  │
        └────┬────────────────────┬────────────────┬─────────┘
             │                    │                │
    ┌────────▼────────┐  ┌────────▼────────┐  ┌──▼──────────┐
    │  ML API Server  │  │  ML API Server  │  │ ML API ...  │
    │    Instance 1   │  │    Instance 2   │  │ Instance N  │
    │                 │  │                 │  │             │
    │ ml_api_scalable │  │ ml_api_scalable │  │   (Scale)   │
    │      .py        │  │      .py        │  │             │
    └────┬────────────┘  └────┬────────────┘  └──┬──────────┘
         │                    │                  │
         └────────────────────┴──────────────────┘
                      │
         ┌────────────┴───────────────┐
         │                            │
    ┌────▼────────┐          ┌────────▼────────┐
    │   CACHE     │          │    DATABASE     │
    │             │          │                 │
    │   Redis     │          │  MySQL / PG     │
    │  (Optional) │          │                 │
    │             │          │ crop_productions│
    │ - Options   │          │ - forecasts     │
    │ - Forecasts │          │ - prediction_   │
    │ - Metadata  │          │   logs          │
    └─────────────┘          │ - model_metadata│
                             └─────────────────┘
```

---

## Data Flow - Prediction Request

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER SENDS PREDICTION REQUEST                           │
│    POST /api/predict                                        │
│    { municipality, crop, year, month, area_planted }        │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. API VALIDATES INPUT                                      │
│    - Required fields present?                               │
│    - Valid municipality/crop/farm_type?                     │
│    - Numeric values in range?                               │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. CHECK CACHE (if enabled)                                 │
│    Key: predict:ATOK:CABBAGE:2024:JAN:10.5                 │
│    Hit? → Return cached result (5-10ms)                     │
│    Miss? → Continue to step 4                               │
└─────────────┬───────────────────────────────────────────────┘
              │ Cache miss
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. GET VALIDATION DATA                                      │
│    Data Layer → Database or CSV                             │
│    - Available municipalities                               │
│    - Available crops                                        │
│    - Farm types                                             │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. PREPARE INPUT FOR MODEL                                  │
│    Create DataFrame with:                                   │
│    [MUNICIPALITY, FARM TYPE, YEAR, MONTH, CROP, Area...]   │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. ML MODEL PREDICTION                                      │
│    Preprocessor → Transform features                        │
│    RandomForest → Predict production_mt                     │
│    (50-80ms)                                                │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. LOG PREDICTION (async)                                   │
│    Database.prediction_logs.insert({                        │
│      crop, municipality, prediction,                        │
│      request_ip, processing_time, ...                       │
│    })                                                       │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. CACHE RESULT (if enabled)                                │
│    Cache.set(key, result, ttl=300)                          │
└─────────────┬───────────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│ 9. RETURN JSON RESPONSE                                     │
│    {                                                        │
│      success: true,                                         │
│      prediction: { production_mt: 123.45, ... },           │
│      processing_time_ms: 85.2                               │
│    }                                                        │
└─────────────────────────────────────────────────────────────┘
```

**Total Time:**
- Cache hit: 5-10ms
- Cache miss: 80-120ms
- Database query: +20-50ms
- ML prediction: 50-80ms

---

## Database Schema Relationships

```
┌──────────────────────────┐
│   crop_productions       │  Historical data
├──────────────────────────┤
│ id (PK)                  │
│ municipality             │◄────┐
│ farm_type                │     │
│ year                     │     │
│ month                    │     │
│ crop                     │◄──┐ │
│ area_planted_ha          │   │ │
│ area_harvested_ha        │   │ │
│ productivity_mt_ha       │   │ │
│ production_mt            │   │ │
│ created_at               │   │ │
│ updated_at               │   │ │
└──────────────────────────┘   │ │
                               │ │
┌──────────────────────────┐   │ │
│      forecasts           │  Future predictions
├──────────────────────────┤   │ │
│ id (PK)                  │   │ │
│ crop                     │───┘ │
│ municipality             │─────┘
│ year                     │
│ production_mt            │
│ confidence_lower         │
│ confidence_upper         │
│ trend_direction          │
│ growth_rate_percent      │
│ generated_at             │
│ created_at               │
│ updated_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   prediction_logs        │  API request logs
├──────────────────────────┤
│ id (PK)                  │
│ municipality             │
│ farm_type                │
│ year                     │
│ month                    │
│ crop                     │
│ area_planted_ha          │
│ predicted_production_mt  │
│ request_ip               │
│ user_agent               │
│ processing_time_ms       │
│ created_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   model_metadata         │  ML model versions
├──────────────────────────┤
│ id (PK)                  │
│ version (UNIQUE)         │
│ model_type               │
│ training_date            │
│ test_score               │
│ cv_score                 │
│ features_used (JSON)     │
│ hyperparameters (JSON)   │
│ performance_metrics      │
│ is_active                │
│ created_at               │
│ updated_at               │
└──────────────────────────┘
```

---

## Cache Strategy

```
┌─────────────────────────────────────────────────┐
│              CACHE LAYERS                       │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Layer 1: Laravel Cache (Optional)              │
│  - Available options (1 hour)                   │
│  - Forecasts (1 hour)                           │
│  - Statistics (30 min)                          │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  Layer 2: ML API Cache (Redis or Memory)        │
│                                                 │
│  ┌──────────────────┐  TTL: 1 hour             │
│  │ Available Options│  (metadata, crops, etc)  │
│  └──────────────────┘                          │
│                                                 │
│  ┌──────────────────┐  TTL: 1 hour             │
│  │    Forecasts     │  (by crop+municipality)  │
│  └──────────────────┘                          │
│                                                 │
│  ┌──────────────────┐  TTL: 5 minutes          │
│  │   Predictions    │  (optional caching)      │
│  └──────────────────┘                          │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  Layer 3: Database Query Cache                  │
│  (Built into MySQL/PostgreSQL)                  │
└─────────────────────────────────────────────────┘
```

**Cache Hit Rates (Expected):**
- Available options: 95%+ (changes rarely)
- Forecasts: 80%+ (stable data)
- Predictions: 30-50% (unique combinations)

---

## Scalability Paths

### Path 1: Single Server (Recommended Start)
```
┌────────────────────────┐
│   Laravel + ML API     │
│   (Same Server)        │
│                        │
│  - MySQL (XAMPP)       │
│  - Memory Cache        │
│  - File fallback       │
└────────────────────────┘

Capacity: ~100 concurrent users
Cost: Minimal
Complexity: Low
```

### Path 2: Separated Services
```
┌──────────────┐         ┌──────────────┐
│   Laravel    │◄───────►│   ML API     │
│   Server     │  HTTP   │   Server     │
└──────┬───────┘         └──────┬───────┘
       │                        │
       └────────┬───────────────┘
                │
        ┌───────▼────────┐
        │  MySQL Server  │
        │  (Shared DB)   │
        └────────────────┘

Capacity: ~200 concurrent users
Cost: Low
Complexity: Medium
```

### Path 3: Load Balanced (High Scale)
```
┌──────────────┐
│   Laravel    │
│   Server(s)  │
└──────┬───────┘
       │
┌──────▼──────────────────────────┐
│      Load Balancer              │
└──────┬──────────┬───────┬───────┘
       │          │       │
┌──────▼────┐ ┌──▼───┐ ┌─▼─────┐
│ ML API 1  │ │API 2 │ │ API N │
└──────┬────┘ └──┬───┘ └─┬─────┘
       │         │        │
       └────┬────┴────┬───┘
            │         │
    ┌───────▼───┐ ┌───▼────────┐
    │   Redis   │ │   MySQL    │
    │  (Cache)  │ │ (Database) │
    └───────────┘ └────────────┘

Capacity: 1000+ concurrent users
Cost: Medium-High
Complexity: High
```

### Path 4: Cloud Deployment (Maximum Scale)
```
┌─────────────────────────────────────────┐
│          Cloud Provider                 │
│  ┌─────────────────────────────────┐   │
│  │   Auto-Scaling Group            │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐    │   │
│  │  │API 1 │ │API 2 │ │API N │    │   │
│  │  └──────┘ └──────┘ └──────┘    │   │
│  └─────────────────────────────────┘   │
│         │                  │            │
│  ┌──────▼──────┐    ┌──────▼──────┐   │
│  │   Redis     │    │   RDS       │   │
│  │  (Cluster)  │    │  (Database) │   │
│  └─────────────┘    └─────────────┘   │
└─────────────────────────────────────────┘

Capacity: Unlimited (auto-scales)
Cost: Pay per use
Complexity: High
Managed: Yes
```

---

## Deployment Diagram

```
┌────────────────── DEVELOPMENT ──────────────────┐
│                                                 │
│  Developer Machine:                             │
│  ┌──────────────────────────────────────┐      │
│  │  python ml_api_scalable.py           │      │
│  │  (Flask development server)          │      │
│  │  http://127.0.0.1:5000               │      │
│  └──────────────────────────────────────┘      │
│                                                 │
│  Database: SQLite or MySQL (XAMPP)              │
│  Cache: Memory                                  │
│  Logs: Console                                  │
└─────────────────────────────────────────────────┘

┌────────────────── PRODUCTION ───────────────────┐
│                                                 │
│  Production Server:                             │
│  ┌──────────────────────────────────────┐      │
│  │  Nginx (Reverse Proxy)               │      │
│  │  ↓                                    │      │
│  │  Gunicorn / Waitress                 │      │
│  │  ↓                                    │      │
│  │  ML API (multiple workers)           │      │
│  │  http://api.yourdomain.com           │      │
│  └──────────────────────────────────────┘      │
│                                                 │
│  Database: MySQL/PostgreSQL (dedicated server)  │
│  Cache: Redis (dedicated or cluster)            │
│  Logs: File + monitoring service                │
│  SSL: Let's Encrypt / CloudFlare                │
│  Monitoring: Grafana, Prometheus, etc.          │
└─────────────────────────────────────────────────┘
```

---

## File Structure

```
Machine Learning/
│
├── Core API Files
│   ├── ml_api.py                    ← Original (legacy)
│   ├── ml_api_scalable.py           ← NEW Scalable version
│   ├── config.py                    ← NEW Configuration
│   ├── database.py                  ← NEW Database layer
│   ├── data_layer.py                ← NEW Data access
│   └── cache.py                     ← NEW Caching
│
├── Database
│   ├── schema.sql                   ← NEW SQL schema
│   └── migrate_csv_to_db.py         ← NEW Migration script
│
├── Configuration
│   ├── .env.example                 ← NEW Config template
│   ├── .env                         ← Your settings (git-ignored)
│   └── requirements.txt             ← Updated dependencies
│
├── Documentation
│   ├── README_SCALABLE.md           ← NEW Full docs
│   ├── QUICKSTART.md                ← NEW Quick start
│   ├── COMPARISON.md                ← NEW Comparison
│   ├── SCALABILITY_UPGRADE_SUMMARY.md ← NEW Summary
│   └── ARCHITECTURE_DIAGRAMS.md     ← This file
│
├── Laravel Integration
│   ├── MLApiService.php             ← NEW Service class
│   └── 2024_11_14_create_ml_tables.php ← NEW Migration
│
├── Setup Scripts
│   └── setup.ps1                    ← NEW Setup wizard
│
└── Model Artifacts (unchanged)
    ├── best_rf_model.pkl
    ├── preprocessor.pkl
    └── *.json metadata files
```

---

This architecture provides a clear, scalable path forward while maintaining backward compatibility with your existing system.
